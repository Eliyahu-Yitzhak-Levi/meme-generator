'use strict'

console.log('utils is connected (meme-editor.utils)');

// function createImageArray(folderPath, numOfImages) {
//     const images = [];
//     const idLength = 3; // Length of the ID number (e.g., "001")

//     // Loop through the specified number of images
//     for (let i = 1; i <= numOfImages; i++) {
//         // Generate the ID with leading zeros
//         const id = String(i).padStart(idLength, '0');

//         // Construct the URL for the image
//         const url = `${folderPath}/${id}.jpg`;

//         // Create an object representing the image and push it to the array
//         images.push({ id: id, url: url, keywords: ['filler1', 'filler2'] });
//     }

//     return images;
// } works fine, problem is the keywords...


// const folderPath = 'meme-imgs/meme-imgs (various aspect ratios)'; // Update this with your folder path
// const numOfImages = 25; // Update this with the number of images in the folder



